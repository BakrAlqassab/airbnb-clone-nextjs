export enum LspdButtonTheme {
  Primary = "primary",
  Secondary = "secondary",
  Outline = "outline",
  Flush = "flush",
}

export enum LspdButtonSize {
  Normal = "normal",
  Small = "small",
}

export enum LspdButtonIconPosition {
  Left = "left",
  Right = "right",
}
